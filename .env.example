# Optional. When omitted, Medusa uses the mock analysis API.
# For local web development with the local vision backend:
VISION_API_URL=http://127.0.0.1:8000

# Optional. Lets phones call the HTTPS vision backend directly for lower latency.
NEXT_PUBLIC_VISION_API_URL=https://your-public-vision-api-url
